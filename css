/* Global styles */
* {
    font-family: 'Kanit', sans-serif;
    box-sizing: border-box;
}

body {
    background-color: #251e10;
    color: goldenrod; /* Set goldenrod as the default text color */
    margin: 0;
    padding: 0;
}

/* Navbar styles */
.navbar {
    width: 100%;
    height: 60px;
    position: fixed;
    top: 0;
    left: 0;
    display: flex;
    align-items: center;
    background-color: #2c1b0c;
    padding: 10px 20px;
    z-index: 1000;
}

.navbar .logo a {
    font-size: 1.5rem;
    font-weight: bold;
    color: goldenrod;
    text-decoration: none;
    padding: 10px 20px;
    border-radius: 8px;
    transition: all 0.3s ease;
}

.navbar .logo a:hover {
    background-color: goldenrod;
    color: #251e10;
}

/* Main content area */
.container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100vh;
    text-align: center;
    padding-top: 80px; /* Adjust space from navbar */
}

/* Header for AI Introduction */
h1 {
    font-size: 3rem;
    margin: 20px 0;
    color: goldenrod; /* Ensure 'What is AI?' heading is goldenrod */
}

/* Section styling */
section {
    padding: 20px;
    max-width: 800px;
    width: 100%;
}

h3 {
    font-size: 1.25rem;
    line-height: 1.6;
    color: goldenrod; /* Ensure all paragraph text is goldenrod */
    margin-bottom: 20px;
    font-weight: 400;
}

/* Link styles for the "Back" button */
.highlight {
    text-decoration: none;
    color: goldenrod; /* Ensure 'Back' button text is goldenrod */
    font-size: 1rem;
    font-weight: 600;
    padding: 10px 20px;
    background-color: transparent;
    border: 2px solid goldenrod;
    border-radius: 8px;
    transition: background-color 0.3s, color 0.3s;
}

.highlight:hover {
    background-color: goldenrod;
    color: #251e10;
}

.highlight:active {
    transform: scale(0.95);
}

/* Responsive styling */
@media screen and (max-width: 768px) {
    h1 {
        font-size: 2rem;
    }

    h3 {
        font-size: 1rem;
    }
}
